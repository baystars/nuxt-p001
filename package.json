{
  "name": "p001-cli",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build && npm run copyfunc",
    "copyfunc": "rimraf functions/.nuxt && cpx .nuxt/dist/**/* functions/.nuxt/dist",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "nuxt": "^2.14.0"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "rimraf": "^3.0.2"
  }
}
